<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          android:versionCode="1"
          android:versionName="1.0">

    <!-- NOTE: minSdkVersion and targetSdkVersion specified in build.gradle -->

    <application
        android:theme="@android:style/Theme.Holo"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name">

        <!-- MAIN ACTIVITY -->
        <activity
            android:name="ca.yorku.eecs.mack.groupproject.main"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- VOWEL- INTRO ACTIVITY -->
        <activity
            android:name="ca.yorku.eecs.mack.groupproject.Vowel_Intro"
            android:exported="true" >
        </activity>

        <!-- K- INTRO ACTIVITY -->
        <activity
            android:name="ca.yorku.eecs.mack.groupproject.K_Intro"
            android:exported="true" >
        </activity>

        <!-- M- INTRO ACTIVITY -->
        <activity
            android:name="ca.yorku.eecs.mack.groupproject.M_Intro"
            android:exported="true" >
        </activity>

        <!-- R- INTRO ACTIVITY -->
        <activity
            android:name="ca.yorku.eecs.mack.groupproject.R_Intro"
            android:exported="true" >
        </activity>

    </application>

</manifest>
